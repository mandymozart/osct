# OSCT documentation site

Built with [Docusaurus](https://docusaurus.io/). Pages are in `docs/`, the sidebar in `sidebars.ts`.

```bash
npm install
npm start       # dev server with live reload
npm run build   # static site → build/
```

For the app itself see the repository `README.md`.
